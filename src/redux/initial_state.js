import { SORT_BY_NAME } from '../various_things/constants';

const initialState = {
    folders: {
        folders: [],
        sortBy: SORT_BY_NAME
    },
    
    message: null
};

export default initialState;